test('Should throw error if outflow does not have due date', () => {
  return expect(false).toBe(true)
})

describe('Outflow Payment Details', () => {
  test('Should throw error if outflow payment type is PIX and has no bank', () => {
    return expect(false).toBe(true)
  })

  test('Should throw error if outflow payment type is PIX and has no bank', () => {
    return expect(false).toBe(true)
  })

  test('Should throw error if outflow payment type is TRANSFER and has no bank', () => {
    return expect(false).toBe(true)
  })

  test('Should throw error if outflow payment type is BOLETO and has no bank', () => {
    return expect(false).toBe(true)
  })

  test('Should throw error if outflow payment type is TED and has no bank', () => {
    return expect(false).toBe(true)
  })
  
  test('Should throw error if outflow payment type is DOC and has no bank', () => {
    return expect(false).toBe(true)
  })
  
  test('Should throw error if outflow payment type is DEPOSIT and has no bank', () => {
    return expect(false).toBe(true)
  })

  test('Should throw error if outflow payment type is DEBIT_CARD and has no card', () => {
    return expect(false).toBe(true)
  })

  test('Should throw error if outflow payment type is CREDIT_CARD and has no card', () => {
    return expect(false).toBe(true)
  })

  test('Should throw error if outflow payment type is MONEY and bank is set', () => {
    return expect(false).toBe(true)
  })

  test('Should throw error if outflow payment type is MONEY and card is set', () => {
    return expect(false).toBe(true)
  })

  test('Should throw error if outflow payment type is PIX and card is set', () => {
    return expect(false).toBe(true)
  })
  
  test('Should throw error if outflow payment type is DEBIT_CARD and bank is set', () => {
    return expect(false).toBe(true)
  })
  
  test('Should throw error if outflow payment type is CREDIT_CARD and bank is set', () => {
    return expect(false).toBe(true)
  })

  test('Should throw error if outflow payment type is TRANSFER and card is set', () => {
    return expect(false).toBe(true)
  })

  test('Should throw error if outflow payment type is BOLETO and card is set', () => {
    return expect(false).toBe(true)
  })

  test('Should throw error if outflow payment type is TED and card is set', () => {
    return expect(false).toBe(true)
  })
  
  test('Should throw error if outflow payment type is DOC and card is set', () => {
    return expect(false).toBe(true)
  })
  
  test('Should throw error if outflow payment type is DEPOSIT and card is set', () => {
    return expect(false).toBe(true)
  })

})
