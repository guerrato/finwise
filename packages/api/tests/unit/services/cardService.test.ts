test('Credit card must have due date', () => {
  return expect(false).toBe(true);
})

test('Card must have issuer bank', () => {
  return expect(false).toBe(true);
})

test('Card must have network', () => {
  return expect(false).toBe(true);
})

test('Card must have issuer bank when network is Credit', () => {
  return expect(false).toBe(true);
})

test('Card must have issuer bank when network is Debit', () => {
  return expect(false).toBe(true);
})