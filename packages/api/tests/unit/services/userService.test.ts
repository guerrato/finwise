describe('User Service validations', () => {
  test('Should create with valid fields', async () => {
    expect(false).toBe(true)
  })
  test('Should throw error for missing mandatory fields', async () => {
    expect(false).toBe(true)
  })
  test('Should throw error for already existing email', async () => {
    expect(false).toBe(true)
  })
  test('Should throw error for invalid email format', async () => {
    expect(false).toBe(true)
  })
  test('Should throw error for invalid password', async () => {
    expect(false).toBe(true)
  })
  test('Should throw error for invalid name', async () => {
    expect(false).toBe(true)
  })
  test('Should throw error for invalid name', async () => {
    expect(false).toBe(true)
  })
  test('Should NOT return password when created', async () => {
    expect(false).toBe(true)
  })
  
})
