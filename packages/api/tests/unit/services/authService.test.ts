describe('Auth Services', () => {
  describe('Sign Up Flow', () => { 

  })

  describe('Sign In Flow', () => {
    test('Should return token valid email and password', async () => {
      expect(false).toBe(true)
    })
    
    test('Token MUST have sub property', async () => {
      expect(false).toBe(true)
    })
    
    test('Token MUST have exp property', async () => {
      expect(false).toBe(true)
    })
    test('Token MUST have exp property', async () => {
      expect(false).toBe(true)
    })
    test('Should NOT allow sign in of NON VERIFIED user', async () => {
      expect(false).toBe(true)
    })
  })

  test('Should return user with valid email and password', async () => {
    expect(false).toBe(true)
  })
  test('Should throw error for invalid email', async () => {
    expect(false).toBe(true)
  })
  test('Should throw error for non-existing email', async () => {
    expect(false).toBe(true)
  })
  test('Should throw error for invalid password', async () => {
    expect(false).toBe(true)
  })
  test('Should return password reset token with valid email', async () => {
    expect(false).toBe(true)
  })
})